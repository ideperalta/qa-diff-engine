{
  "name": "qa-diff-engine",
  "version": "1.0.0",
  "description": "Visual Diff API for QA Checker",
  "main": "server.js",
  "scripts": {
    "build": "npm install && npx playwright install --with-deps chromium",
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "pixelmatch": "^5.3.0",
    "playwright": "^1.42.1",
    "pngjs": "^7.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}